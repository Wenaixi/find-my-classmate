# FindMyClassmate 领域词汇表

> 本文件是项目的领域术语表（glossary），不是规范也不是脚手架。它只收录项目独有的领域概念，通用编程概念不收。随术语落定惰性维护。

## 名单域

- **名单（roster）**：按年段组织的学生编班数据源。源文件为 `data/高一.json`、`data/高二.json`、`data/高三.json`（按需只放实际存在的年段），运行期可热重载。
- **学生（student）**：名单中的一名成员，对外只暴露 `name`、`grade`、`class` 三个字段（隐私红线）。内部承载归一化姓名、班号、年段序等派生字段，永不序列化。
- **年段（grade）**：高一 / 高二 / 高三（或 `高1/高2/高3` 别名）。按自然顺序声明于 `knownGrades`，扩展年段只需在末尾追加。
- **班级（class）**：年段内编号（1~N），由班级名（`1班` 或汉字数字 `一班`）解析为班号。
- **查询 token（query token）**：查询词被分隔符（中英文逗号、顿号、加号、连续空白）切分后的最小单元，可解析为姓名 / 年段 / 班级条件。
- **姓名条件（name condition）**：查询中至少一个 token 解析为姓名匹配词。前端「纯年段/班级查询」提示由此判定。
- **无条件查询（unconstrained query）**：解析后不产生任何查询条件的输入。典型形态是纯分隔符（「、」「,」「+」）与纯空白——它们在归一化后产出零个 token，既非空查询也无条件可施加。**此类输入必须退化为空结果集，绝不能退化为全校检索**：条件全部落空时匹配循环不施加任何约束，等于向任意用户返回整份名单。判定依据是「解析后是否存在条件」，不是「原始串剥空白后是否为空」——两处归一化不一致曾使纯分隔符漏网。

## 数据可用性

- **数据可用性（data availability）**：名单当前是否可对外服务。由热重载状态机单点判定；数据损坏/缺失时 `/api/health` 返回 503 degraded、`/api/search` 返回 500。
- **自恢复时机（recovery timing）**：热重载状态机判定「何时应探测文件指纹 / 何时应重试失败重载」的单一时机基准——探测节流（1 秒窗口）与失败冷却（2 秒窗口）共用同一条时间线，收敛为单点判定。
- **时钟注入（clock injection）**：把 `now func() time.Time` 作为构造参数注入状态机（限流器、热重载），使时间推进在测试中可控；注入点单点负责，不允许从外部改写。

## 查询会话

- **查询会话（search session）**：一次查询及其在途请求的竞态上下文。由 session 层管理 requestId + abort，「过期响应被丢弃」由判别联合显式化。
- **查询控制器（search controller）**：把「发起并展示一次查询」的编排收进一根深接口的模块。组件只消费 `state`（渲染派生）与 `controller`（意图动作）两面。
- **交互语义（input semantics）**：输入框的键盘/输入法行为（IME 组合期间 Enter 不提交、Escape 清空、组合事件与提交守卫），与滚动时机一起构成组件的交互状态线。
- **结果区段（result section）**：查询状态到「该渲染哪一块界面结构」的映射，取值 list / loading / empty / error / null（null 表示不渲染结果区）。与「是否滚动定位」派生自同一处判定，二者不可能错位；此前它们在组件里各写一份且状态集合恰好互补，新增查询状态时只改一处编译器不报错。

## 契约

- **契约语料（contract corpus）**：`docs/query-contract.json`，跨语言解析契约的唯一事实源，前后端测试各自消费同一份语料，任一侧漂移即失败。
- **契约常量对拍（constant parity）**：双端各持一份数值常量（分页/上限/超时），由 `contract_constants_test.go` 读取前端常量断言一致。
